using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Spell
{
    /*
    public SpellStats stats;

    void OnCollisionEnter2D(Collision2D other)
    {
        if(other.gameObject.tag == "Enemy")
        {
            //other.gameObject.GetComponent<EnemyAI>().health -= stats.damage * transform.parent.GetComponent<PlayerMovement>().attackMultiplier;
        }

        //Destroy(gameObject);
    }

    void OnCollisionStay2D(Collision2D other)
    {
        if(other.gameObject.tag == "Enemy" && stayDamage)
        {
            other.gameObject.GetComponent<EnemyAI>().health -= stats.stayDamage * transform.parent.GetComponent<PlayerMovement>().attackMultiplier;
        }
    }
    */
}
